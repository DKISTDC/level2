#!/bin/bash
set -euo pipefail

# export METADATA_STORE_API_DIR="${METADATA_STORE_API_DIR:-../metdata-store-api}"

# cleanup() {
#   echo "Shutting down: metdata-store-api"
#   (cd $METADATA_STORE_API_DIR && docker-compose down)
# }
# trap cleanup EXIT

# (cd $METADATA_STORE_API_DIR && docker-compose --profile full up --build -d)

docker-compose up

